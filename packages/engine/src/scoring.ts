export type PerformanceStats = {
  progress: number;
  accuracy: number;
  streak: number;
  longestStreak: number;
  points: number;
};

export type BandScore = {
  total: number;
  hype: number;
};
